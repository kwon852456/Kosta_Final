git config --global user.name "자신의 닉네임"
git config --global user.email "자신의 이메일"  //깃 최초 시그니처 생성
git config --global core.editor vim // 깃 편집기 변경
git add "file name" // add some file to be added to rep
git commit // commit changes on added files
git log // see logs

git log -p // see details between commited version
git log 'commit id' // see previous contents untill commit content of id
git diff 'commit id'..'commit id' // see changes between commits
git diff // see changes between previous staged file and contemporary file

git branch
git branch "새로운 브랜치 이름"
git branch -d
git branch -D
git checkout "전환하려는 브랜치 이름"
git checkout -b "생성하고 전환할 브랜치 이름" 
